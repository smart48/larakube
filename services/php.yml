apiVersion: v1
kind: Service
metadata:
  name: php-service
  namespace: smt-local
  labels:
    tier: backend
spec:
  selector:
    app: php
    tier: backend
  ports:
  - protocol: TCP
    port: 9000